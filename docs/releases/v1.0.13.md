# LoomTV 1.0.13

## Fixes

- Fixes macOS launches that appeared to do nothing when LoomTV was already running in the background.
- Adds a single-instance lock so opening LoomTV again brings the existing window forward instead of starting another copy.
- Keeps the macOS app lifecycle healthy after closing all windows, so Dock activation restores LoomTV cleanly.
