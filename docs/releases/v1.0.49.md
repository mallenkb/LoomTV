# Loom Media Server 1.0.49 Release Notes

Loom Media Server 1.0.49 republishes the current desktop build with fresh platform installers and updater metadata.

## Verification

- `git diff --check` passes locally.
- Tagged GitHub Actions will run desktop lint, typecheck, tests, packaging, and packaged runtime verification for Windows, macOS, and Linux.
