# LoomTV 1.0.21

This release validates the updater path from the fixed 1.0.20 build.

## Updates

- Publishes a fresh packaged build for testing restart-to-update from LoomTV 1.0.20.
- Confirms release packaging keeps renderer asset paths relative so the app does not load to a blank window after restart.

## Verification

- Used to verify that the downloaded update closes LoomTV, installs the new version, relaunches from `/Applications`, and renders without a blank window.
