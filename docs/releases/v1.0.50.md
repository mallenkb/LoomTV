# Loom Media Server 1.0.50 Release Notes

Loom Media Server 1.0.50 fixes the desktop release pipeline.

## Fixed

- Aligns GitHub Actions' pnpm setup with the workspace's declared pnpm 11.13.1 toolchain.
- Allows macOS, Windows, and Linux installer jobs to proceed past dependency setup.

## Verification

- `git diff --check` passes locally.
- Tagged GitHub Actions will run desktop lint, typecheck, tests, packaging, and packaged runtime verification for Windows, macOS, and Linux.
