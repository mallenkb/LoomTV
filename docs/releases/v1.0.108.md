# LoomTV 1.0.108 Release Notes

LoomTV 1.0.108 hardens desktop updates and release security validation.

## Highlights

- supports safe updates for legacy ad-hoc-signed macOS installations while preserving Developer ID identity checks
- makes fallback macOS signing consistent and cleans temporary update directories reliably
- updates all affected `js-yaml` paths to the patched 4.3.1 release so production dependency auditing passes
