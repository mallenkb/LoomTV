# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code. The app currently pins Expo SDK 54; record a separate upgrade plan before consulting a newer SDK as implementation guidance.
